입력 -> 처리 -> 출력
글꼴 Consolas

(컴퓨터) 프로그램
	(소스코드로) 잘 짜여진 틀

소스코드
	컴퓨터에게 내리는 명령을 뜻하는 문장

운영체제(OS)
	Windows, IOS, MacOS, Android, ...
	하드웨어에 적절한 전기신호를 흘려주는 역할

	프로그램 -> 운영체제 -> 적절한 하드웨어

JAVA
	컴파일 언어
	객체지향 언어

컴파일(Compile)
	고급언어(사람의 언어)를 기계어(컴퓨터 언어)로 바꿔주는 작업
	목적파일(번역문 파일) 생성 / JAVA : 클래스 파일(.class)

	System.out.println()		101100110101010111

컴파일러
	javac.exe
	컴파일을 해주는 프로그램

자바 프로그램
	JAVA 프로그램
	     ↓
	JVM(자바 가상 운영체제)
	     ↓
	  운영체제
	     ↓
	  하드웨어

	JAVA는 JVM과 소통하는 언어이기 때문에 운영체제가 바뀌어도 소통이 가능하다.
	이러한 것을 이식성이 좋다라고 한다. 웹 개발에 최적화된 언어이다.

JVM(Java Virtual Machine)
	자바 가상 운영체제
	JAVA 프로그램을 실행해준다.
	땅

JRE(Java Runtime Environment)
	자바 실행 환경
	JVM을 생성해주는 부분과 JVM에서 실행할 때 필요한
	라이브러리 파일들, 기타 파일들을 가지고 있다.
	흙, 공기, 햇빛, 물, ...

JDK(Java Development Kit)
	자바 개발 도구
	JAVA 프로그램을 생성할 수 있게 해주는 역할
	JRE 외에도 개발에 필요한 도구들을 가지고 있다.
	삽, 비료, ...
-------------------------------------------------------------------------
구글링, 복붙, 툴의도움
JDK 설치
	jdk 8 download 검색 > 첫번째 링크 클릭 > 아래로 내려서 Java 8 클릭
	> 그 아래에 운영체제 선택 > 설치파일 다운로드 > 로그인

	아이디 : tedhan1204@gmail.com
	비밀번호 : Asdf1234!

	설치파일 실행해서 설치 진행(경로는 Program Files 안에 Java 폴더인지 확인)
	> 윈도우 검색에다 환경 검색 > 시스템 환경 변수 편집 클릭 > 환경 변수 버튼 클릭
	> 아래쪽에 새로만들기 클릭해서 변수 만들기
	변수명 : JAVA_HOME
	변수 값 : jdk 설치된 경로(C:\Program Files\Java\jdk1.8.0_311)
	> Path변수 찾아서 편집 > 새로운 창에서 새로 만들기 클릭 >
	%JAVA_HOME%\bin 작성 후 확인 > 모든 창 닫고 cmd 실행 > javac -version 명령어 수행

통합 개발 환경(IDE)
	Eclipse 19-12버전 이용

	https://www.eclipse.org/downloads/packages/release/2019-12/r
	Download 버튼 클릭하지 말고 아래의 .zip파일로 다운로드 받기

자바 프로그램의 구조

	프로젝트
		패키지
			클래스(맨 앞글자가 대문자)
				메소드(이름 뒤에 소괄호)
					소스코드

{}
	영역, 바디
	해당하는 클래스, 메소드 등의 영역을 나타낸다.
	*현재 선언중(생성중)임을 나타낸다.

주석
	해석하고 싶지 않은 문장 앞에 사용한다.

	//한줄주석
	/*
	여러줄
	주석
	*/

	1. 소스코드에 설명하는 글을 달 때(개발자들간의 소통)
	2. 기존에 사용하던 소스코드를 더이상 사용하지 않을 때

출력
	개발자가 사용자에게 값을 보내어 표현해주는 행위

출력 메소드
	System.out

	.print(값)
		괄호 안에 있는 값을 그대로 출력
		마지막에 줄바꿈을 하지 않는다.

	.println(값)
		print()와 동일, 마지막에 줄바꿈을 한다.

	.printf()

제어문자
	따옴표 안에 존재한다.
	\와 함께 쓰인다.

	\n : Enter(줄바꿈)
	\t : Tab(공백)
	\" : "
	\' : '
	\\ : \

.printf()

	System.out.printf("서식문자열",값1,값2,...);

	서식문자열 : 서식문자가 포함된 문자열
	ex) "%fcm" --> 실수한개 뒤에 "cm"라는게 붙은 문자열

	뒤에 올 값들은 앞에 문자열에 있는 서식문자대로 따라와야 한다.

	printf("키 : %fcm",170.24); ---> 키 : 170.24cm


서식문자(format 문자)
	따옴표 안에 존재한다.
	%와 함께 쓰인다.

	%d : 정수
	%c : 문자
	%s : 문자열
	%f : 실수
	%% : %













